{
  "name": "ui",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "pnpm build:code && pnpm build:types",
    "build:code": "swc src -d dist --source-maps",
    "build:types":"tsc --outDir dist",
    "lint": "eslint *.ts*"
  },
  "dependencies": {
    "themes.base": "workspace:*"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.59",
    "@swc/core": "^1.3.24",
    "@types/react": "18.0.26",
    "@types/react-dom": "18.0.10",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "react": "18.2.0",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.2"
  }
}
